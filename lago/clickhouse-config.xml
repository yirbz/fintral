<clickhouse>
    <listen_host>127.0.0.1</listen_host>
    <logger>
        <level>warning</level>
        <log>/var/log/clickhouse-server/clickhouse-server.log</log>
        <errorlog>/var/log/clickhouse-server/clickhouse-server.err.log</errorlog>
        <size>50M</size>
        <count>2</count>
    </logger>
    <path>/var/lib/clickhouse/</path>
    <tmp_path>/var/lib/clickhouse/tmp/</tmp_path>
    <user_files_path>/var/lib/clickhouse/user_files/</user_files_path>
    <access_control_path>/var/lib/clickhouse/access/</access_control_path>

    <!-- Hard cap at 128 MB — sufficient for subscription-only billing -->
    <max_server_memory_usage>134217728</max_server_memory_usage>
    <!-- Limit concurrent queries to reduce memory spikes -->
    <max_concurrent_queries>5</max_concurrent_queries>
</clickhouse>
